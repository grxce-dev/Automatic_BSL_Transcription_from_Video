# If most of the array is zero it is a bad sample. 50%+ zeros = bad
# Have Stable Landmark Movement: You should see a smooth motion curve not chaotic spikes

#import matplotlib.pyplot as plt

#print(np.sum(data == 0))
#plt.plot(data[:, 0])  # first feature across 30 frames
#plt.show()

